/** Số cột khóa học trên desktop (xl) — dùng tính số item mỗi hàng. */
export const HOME_COURSE_COLS = 5

/** "Dành cho bạn" — hiển thị 1 hàng ban đầu. */
export const HOME_COURSE_ROWS_RECOMMENDED = 1

/** Các section grid khác — hiển thị 2 hàng ban đầu. */
export const HOME_COURSE_ROWS_GRID = 2

/** Grid khóa học: mobile 2 cột, lg 3 cột, xl 5 cột. */
export const COURSE_GRID_BASE_CLASS =
  'grid grid-cols-2 gap-3 sm:gap-6 lg:grid-cols-3 xl:grid-cols-5'

export const COURSE_GRID_CLASS = `${COURSE_GRID_BASE_CLASS} pb-2`

export const COURSE_GRID_CLASS_LOOSE =
  'grid grid-cols-2 gap-x-3 gap-y-6 sm:gap-x-6 sm:gap-y-8 lg:grid-cols-3 xl:grid-cols-5'
